# 작업 계획서 템플릿 (Speech-M)

## 1) 작업명
- 예: 잡 공고 AI 적합도 배치 평가 개선

## 2) 목표 (Why)
- 이번 작업에서 해결할 핵심 문제를 1~2문장으로 정의한다.
- 사용자 가치(운영 효율, 정확도, 안정성)와 직접 연결해 작성한다.

## 3) 범위 (In / Out)
### In Scope
- 이번 작업에서 실제로 수정/추가할 기능과 파일 범위를 명시한다.

### Out of Scope
- 이번 작업에서 의도적으로 제외하는 항목을 명시한다.

## 4) 제약사항
- Next.js 호환성: 프레임워크 동작은 로컬 docs 기준으로 검증한다.
- 크롤링/배치: 서버리스 타임아웃을 고려해 비동기 패턴을 우선한다.
- 보안/DB: 민감 데이터는 RLS 보수 정책을 유지한다.
- AI 필터링: 상세 기준이 미정이면 구현 전에 반드시 질의/확정한다.

## 5) 구현 전략 및 트레이드오프
1. 선택한 접근
   - 선택 이유를 성능/복잡도/운영성 관점에서 간단히 기록한다.
2. 대안 접근
   - 왜 이번 범위에서 배제했는지 명확히 기록한다.

## 6) 완료 기준 (Definition of Done)
- [ ] 기능 요구사항이 정상 동작한다.
- [ ] 실패/예외 시나리오가 처리된다.
- [ ] 기존 핵심 흐름에 회귀가 없다.
- [ ] 운영자가 결과를 검증할 수 있는 로그 또는 상태가 남는다.

## 7) 검증 계획
- 수동 검증:
  - 관리자 화면/액션 기준 핵심 시나리오를 점검한다.
  - HITL 흐름(명확 케이스 자동 처리, 애매 케이스 수동 검토)을 점검한다.
- 자동 검증:
  - lint, 타입체크, 테스트(존재 시)를 수행한다.

## 8) 리스크 및 롤백
- 예상 리스크: 외부 소스 구조 변경, LLM 응답 변동, 타임아웃.
- 롤백 전략: 플래그/환경변수/이전 배치 경로 복구 방법을 명시한다.

## 9) 결과 기록
- 변경 파일 목록
- 검증 결과
- 후속 작업(필요 시)

## 10) 작업 종료 보고 & 자동 커밋
- 종료 보고 템플릿:
  - 작업 요약 / 검증 결과 / 변경 상태(`staged`, `unstaged`, `untracked`) / 추천 커밋 문구
- 자동 커밋 실행 순서:
  1. `git status` -> 2) `git diff` -> 3) 커밋 문구 확정 -> 4) `git add` -> 5) `git commit` -> 6) `git status`
- 커밋 메시지 규칙:
  - 제목 형식: `<type>: <한국어 설명>` (예: `feat: 잡 공고 AI 적합도 배치 실행 버튼 추가`)
  - 본문은 why 중심 2~3줄(선택)
- 보안 예외:
  - `.env*`, 키/토큰/자격증명 파일은 커밋 대상에서 제외
- 실패 처리:
  - 훅 실패 시 원인/수정/재시도 결과를 종료 보고에 추가
